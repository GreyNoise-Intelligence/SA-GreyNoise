[greynoise_identify_noise]
display_location = field_menu
fields = dest, dvc, src, dest_ip, src_ip, dvc_ip, orig_src, orig_dest, host, source, dest_translated_ip, src_translated_ip
label = Identify Noise
link.method = get
link.target = blank
link.uri = /app/SA-GreyNoise/live_investigation?form.tkn_ip_address=$@field_value$&form.redirect=redirect
type = link

[greynoise_search_similar]
display_location = field_menu
fields = dest, dvc, src, dest_ip, src_ip, dvc_ip, orig_src, orig_dest, host, source, dest_translated_ip, src_translated_ip, greynoise_ip
label = Search for Similar Noise IPs
link.method = get
link.target = blank
link.uri = /app/SA-GreyNoise/similar_ip_lookup?form.tkn_ip_address=$@field_value$&form.redirect=redirect
type = link

[greynoise_search_timeline]
display_location = field_menu
fields = dest, dvc, src, dest_ip, src_ip, dvc_ip, orig_src, orig_dest, host, source, dest_translated_ip, src_translated_ip, , greynoise_ip
label = Get Noise IP Timeline
link.method = get
link.target = blank
link.uri = /app/SA-GreyNoise/noise_ip_timeline?form.tkn_ip_address=$@field_value$&form.redirect=redirect
type = link
